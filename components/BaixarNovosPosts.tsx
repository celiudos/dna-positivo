import { useDispatch } from "react-redux";

export default function BaixarNovosPosts() {
  const dispatch = useDispatch();

  // useEffect(() => {
  //   async function init() {
  //     const posts = await BaixarPostsDoBloger.getTodosSelecionados("4");
  //     dispatch(postsNovosAction(posts));
  //   }
  //   init();
  // }, [dispatch]);

  return null;
}
